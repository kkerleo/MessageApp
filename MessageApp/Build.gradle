plugins {
    id 'java'
}

group 'com.ubo.tp.message'
version '1.0.0'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    // Ajoutez ici vos dépendances
    // Par exemple : implementation 'org.apache.commons:commons-lang3:3.12.0'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'main.java.com.ubo.tp.message.Main'
        )
    }
}
